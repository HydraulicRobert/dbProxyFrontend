export function UiHeader({intVersion}){
    return(
        <header id="HeaderMain">
            WELCOME TO NOTIFICATION PROXY TERMINAL<br/>
            Notification Proxy Protocol v.1.{intVersion} 
        </header>
    )
}
